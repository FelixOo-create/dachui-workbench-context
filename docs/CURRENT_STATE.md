# 大锤的工作台当前状态

更新时间：2026-08-31 15:57。本文是本仓库唯一的当前状态入口；历史交接和旧截图不得覆盖本文。

## 当前产品

- Project / Git Root：`E:\Vibecoding\大锤的工作台`；GitHub 上下文快照与正式目录相互隔离。
- 当前版本为 `0.5.1`。正式用户入口是 `启动.bat`。
- 固定七场景为今日、待办、时间块、习惯、记录册、画布和工具。顶部导航仍显示“时间块”，其当前主页面已经调整为“今日专注”。
- 今日首页包含常用入口、今日待办、日历和专注计时组件。
- 待办 V2、今日专注、习惯 V2、纪念册 V2 已连接现有业务接口；工具中心直接在主内容区渲染。
- 画布入口当前禁用并明确标为后续能力，不应从概念图推断其已经完成。
- 当前视觉为近黑、低饱和炭灰体系，使用少量薄荷绿和暖金状态色。

## 今日专注现状

- 番茄钟支持开始、暂停、继续、重置和完成记录。
- 沿用原时间块的活动与分类标签。
- 完成的番茄自动进入一日专注时间流。
- 页面按标签汇总投入，并提供仅基于真实记录的当日摘要。
- 不使用虚构效率评分、AI 总结或无数据来源的建议。
- “补记一段”会进入现有时间记录视图，用于手工补充记录。

## 数据与项目边界

- Registry 模板位于 `data\tools\*.json`；用户编辑保留在 Electron `userData\data`。
- 日程数据库位于 `userData\data\schedule\todo-calendar.db`；纪念册位于 `userData\memories`；书签用户数据位于 `userData\data\bookmarks`。
- 不读取或提交真实数据库、日志、密钥、用户图片或其他隐私数据。
- 外部项目继续由各自目录拥有，Workbench 只提供统一入口。
- Taskboard 工作流当前暂停。

## 验证与产物状态

- `npm run typecheck`：通过。
- `npm test -- --run`：21 个测试文件、89 项测试通过。
- GitHub 快照中的源码、测试、脚本、配置和资源已与正式目录逐文件校验；除快照专用文档外，内容一致。
- 2026-08-31 15:54–15:57 已通过 `npm run dev:web` 重新渲染当前页面并生成隔离截图，见 `docs/CURRENT_UI/`。
- 当前 EXE/ZIP 生成于 14:02，而三个跨页面样式文件在 14:11 又有修改。因此该安装包早于当前源码，不能作为最新视觉证据。
- Web 截图只证明 Renderer 结构可渲染。正式 EXE 下的窗口尺寸、DPI、托盘、显示器恢复和系统交互仍需单独验收。

## 当前协作方式

1. 用户与 Sol 讨论需求和产品方向。
2. 需要视觉探索时，由 GPT 生图作为可选附加项；不是每轮必需。
3. 用户确认方案后，自行把预览图和精简交接提示词交给 Luna。
4. Luna 负责真实代码实施、测试、构建和运行截图，不自行决定产品方向。
5. Sol 集中检查功能正确性和视觉还原度，不因每个小改动反复发送任务或逐点验收。

## 尚未最终验收

- 当前页面仍需用户逐项接受；已有截图和测试不代表全部视觉已经定稿。
- 首页、工具中心及跨页面统一仍可继续优化。
- 画布仍未作为完整功能开放。
- 当前正式安装包落后于 14:11 的最后一轮样式修改。
