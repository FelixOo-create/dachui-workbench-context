# 界面与交互截图索引

## 唯一当前入口

当前实现截图只认：[`docs/CURRENT_UI/README.md`](CURRENT_UI/README.md)。

这些截图于 2026-08-31 15:54–15:57 从当前源码的隔离 Web 运行态生成，使用预览数据，不读取真实数据库。它们证明当前 Renderer 的结构与视觉，不证明正式 EXE、DPI、托盘或系统动作已经完成验收。

## 历史参考，不是当前界面

以下目录继续保留用于理解设计演进，但 GPT 不得把其中图片当作当前实现：

- `docs/视觉参考/全界面统一-审计/`：2026-08-30 中午的旧审计截图。即使文件名包含 `final-*`，也不是当前最终页面。
- `docs/视觉参考/2026-08-30-v0.5.0-*-实际预览.png`：版本 0.5.0 阶段记录。
- `docs/视觉参考/home-canvas-v2-prototype/`：首页代码原型；其中天气和“接下来”等内容不是已确认功能。
- `docs/视觉参考/todo-v2-react-preview/`：待办 V2 早期结构和交互预览。
- `docs/视觉参考/habits-v2-react-preview/`：习惯 V2 早期预览和生产接入过程证据。
- `docs/视觉参考/workspace-scenes-v2-prototype/`：统一壳层概念原型。
- `docs/视觉基线/纪念册/`：批准方向、早期实际截图和问题对照。
- `docs/视觉基线/今日专注/approved-default.png`：批准方向，不是当前运行截图。

## GPT 使用规则

1. 先读 `docs/CURRENT_STATE.md`，再读 `docs/CURRENT_UI/README.md`。
2. 当前实现与历史图片冲突时，以 `CURRENT_UI` 为准。
3. 历史图只能用于解释设计来源，不能证明功能已经存在或仍保持原样。
4. 不使用预览数据和演示数字推断真实用户数据。
5. 不从概念图推断不存在的业务功能。
6. 最终实施验收以新的真实运行截图和业务验证为准。
